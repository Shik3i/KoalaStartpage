{
  "name": "koala-startpage",
  "version": "1.0.0",
  "description": "Security and performance optimized Koala Startpage",
  "private": true,
  "scripts": {
    "build": "tailwindcss -i ./style.src.css -o ./style.css --minify",
    "watch": "tailwindcss -i ./style.src.css -o ./style.css --watch"
  },
  "devDependencies": {
    "tailwindcss": "^3.4.1"
  }
}
